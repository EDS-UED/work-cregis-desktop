---
description: work-cregis-desktop 唯一业务规范 — Desktop 客户端、EDS 边界、i18n、顶底毛玻璃、Popup、场景定稿
globs: **/*
alwaysApply: true
---

# work-cregis-desktop 业务集成规范

> Desktop 客户端（非 Showcase/Website 壳）。入口 `agents.mdc`；EDS 真源 `../eds-desktop/.cursor/rules/work.mdc`、`packages/tokens/spec/**`。

---

## 0. 速查

| § | 主题 | 关键路径 |
|---|------|----------|
| 1 | Desktop-only、禁止复制 | — |
| 2 | EDS 边界、dev、list-field、推送 | `listFieldCryptoSampleAddresses.ts` |
| 3 | DOM、Popup 挂载 | `AppPopupOverlayHost` |
| 4 | Eg* 契约、场景组件、废弃 API | EDS §17 |
| 5 | i18n | `uiTextZhCN.ts`、`translateUiText.ts` |
| 6 | Token、千分位、scrim、Layout 详情、Apply_Item、网络名 | `layoutChromePage*.module.css` |
| 7 | Popup、批签、多签、Shell Debug、DataList | `usePopupShellLifecycle.ts`、`DataList.vue` |
| 8 | 校验 PR | `verify-*.mjs` |

---

## 1. 全局硬约束

### 1.1 Desktop-only

**【必须】** 仅用 `@eds/desktop-tokens`、`@eds/desktop-animations`、`@eds/desktop-components`；排版优先 `.typography-*`（主入口已含）；`--eds-*` 仅单轴覆盖；dev/build alias EDS 源码。

**【禁止】** `@eds/website-*`；从 showcase 抄 CSS/集成/token；spec 外 token（如 `--eds-footnote-medium-*`→`--eds-footnote-*`）；私造 `--*`（仅白名单/`token-allowlist.business.json`）；裸 px/hex/数字 font-weight；覆盖 `Eg*` 根或内部 DOM；`@import '@eds/desktop-components/style.css'`（dist 快照）。

排版真源：`spec/text/styles.json`、`typography/semantic.json`。样式用 Desktop 语义 token+组件 props；交互优先 EDS 组合（`EgFlotation` 等）。

### 1.2 禁止复制

**【禁止】** 任何复制：`showValueCopy`、HashLikeLine/复制、`CryptoAddressSide`、clipboard、复制按钮/Menu；业务区 `user-select:text`（输入框等 `global.css` 白名单除外）。`installPageCopyGuard()` 拦截 copy/cut；`.app-preview` 与 teleport 的 tooltip/flotation 全局禁选。

**【必须】** 哈希/编号/地址/金额只读；溢出→`EgListFieldOverflowText`/`EgTooltipOverflow`（无复制）；`copyOnRowHover=false`。`boundary-selector`/`close-on-scroll` 在引用库组件内配，业务勿重复。

---

## 2. 仓库与工程

### 2.1 EDS ↔ 业务

eds-desktop 改→本 App 跟；本 App 改→不写回。**对齐引用库**：行为以 `../eds-desktop/packages/**` 为准；Showcase `ListFieldPreviewPanel.vue` 仅集成参考；改组件先读引用库再改业务。

| 可改 | 禁止改 |
|------|--------|
| `src/**`、`token-allowlist.business.json`、`scripts/verify-*.mjs` | `../eds-desktop/packages/**`、`showcase/**` |

Popover/token/组件 API→eds-desktop 改。消费 link+ Vite alias→源码。**同步**（仅用户要求）：eds-desktop build→`pnpm install`→重启 dev；仅 tokens/components+可 sync list-field。

### 2.3 dev

UI 改后 `pnpm dev` 保存即 HMR；**禁**只 build 不 dev。`src/**` 确认 dev 在跑；EDS 走 alias 勿改 dist。

### 2.4 list-field 本地扩展

`listFieldCryptoSampleAddresses.ts` 含业务扩展（`getPinnedAddressForRow`、`ton`/`sui`、`familyOverride`、23 地址池）；sync skip；**禁** showcase 整文件替换；手动 diff 合并；跑 `verify-list-field-crypto.mjs`（误改会致 Tasks 整页空白）。

### 2.5 Git 推送（双仓 + Pages）

Pages CI pin 远程 eds-desktop；未推 EDS 或 pin 不存在→Deploy 失败。

**「推/推送」= 全量 GitHub + Pages，一点不剩**：两仓 commit 全推远程、工作区干净、CI pin=远程 eds-desktop HEAD、Pages 由 push 触发 CI。

| Agent 禁 | Agent 必须 |
|----------|------------|
| 主动 `git push`（含 force/upstream） | 只备 commit 说明、预检、可复制 push 命令；**用户终端推** |
| 只改 pin 不推 EDS；pin 用本地 SHA | 推前一次验完 |
| 跑/等 `preview:pages`（4174） | UI 在 `pnpm dev`（4173）目测 |
| 有待提交却当可推 | 先 commit 干净 HEAD |

两仓：`../eds-desktop` EDS→远程 `main`；本仓业务+`Checkout eds-desktop` ref=该 SHA。推前：① 两仓干净 ② `pnpm typecheck`+相关 verify ③ pin 远程存在且=eds-desktop HEAD。4174 非 Agent 门禁。未要求 commit/push **不得**提交或推送。

---

## 3. DOM 与挂载

```
#app（居中 clamp，非业务视口）
└ .app-preview.desktopTokens（1280×800，Tooltip container，业务边界）
  ├ EgContainer → EgLayout → 业务
  └ AppPopupOverlayHost（与 Container 同级）
```

**【必须】** 业务 UI/token/浮层限 `.app-preview` 子树；`.desktopTokens` 包裹层（非 `Eg*` 根）；Popup host 同级 Container（遮罩 NavBar+ModuleMenu+主内容）；`EgPopup` `v-if=open` 或关闭时卸 host；懒挂载→`usePopupShellLifecycle`（§7.1）。`body` 的 `--cregis-shell-*` 外圈仅模拟宿主，非客户端内容。**【禁】** Popup 挂业务 subtree；`#app` 作业务根或挂全局浮层。

`EgPopup` 遮罩=父容器 100%。`EgDetail @close`→**仅** `popupOpen=false`；`emit update:open`+清数据在 EgPopup `@close`/`onClosed`。

样式：`desktop-components-scope.css`（EDS global+interactionHoverTooltip，不 import dist）。

---

## 4. 组件契约

语义 props、`v-model`、slot。**禁**手搓 Tooltip+菜单代 `EgFlotation`；拆改组件 DOM。Tooltip `maxWidth` 勿覆盖：OverflowText 360；DataListCellOverflow 240/480。

### 4.1 场景组件（EDS §17）

| 场景 | 用 | 禁 |
|------|----|----|
| 确认/引导/备注 popover | `EgConfirm/Guidance/RemarkPopover` | 手搓 Tooltip+Popover+Button |
| 矿工费 | `EgGasFeePopover`（bitcoin/ethereum/ton/tron） | 直接 MinerFee*Panel |
| Tag | `EgStatus/Colorful/BusinessTag` | `EgTag family=…` |
| Popup | `EgDetail/Dialog/VerifyPopup` | `EgPopup uses=detail/dialog/verify` |
| Dialog | `EgSymbol/BusinessDialog` | `EgDialog type=symbol/compose` |
| 单因子校验 | `Eg*Verify` 系列 | `EgVerify type=single-*` |

例外：`EgPopup uses=custom`、`EgDialog type=standard`、`EgMinerFeeBatchStubPanel`。壳层 `EgCregisNavBar`/`EgCregisModuleMenu`/`EgUdunModuleMenu`（`:translate=ui`）；禁手拼 nav/menu attrs；Tasks badge 仅 `:groups`。

### 4.2 已删 API

`EgButton variant=*`→`ButtonVariant`；`EgBatchBar action-label`→`labels`；`EgModuleMenuItem level/active`→`tier/subitem`/`focused`；`EgTooltip micro-float` 内建；`EgCryptoSymbol show-entry-badge`→`entry-badge`。

---

## 5. i18n

1. zh-cn 真源 `uiTextZhCN.ts`；en/zh-TW 仅 Excel+`collaborationLocaleOverrides.ts`；Agent 禁自译。
2. 表外 key→三语均 catalog 简体。
3. UI 固定文案→`ui()`/`translateUiText()`；**字段 value 禁 ui()**。
4. en：标签 Title Case；段落 Sentence case；`I18N_DOC_EN_LABELS` 专业词原样；同义 zh 不同 en→用 EN_LABELS，**禁** `COLLABORATION_KEY_RENAMES`。
5. Alert：`EgPopup uses=dialog dialog-type=standard`；禁 `reminder`。

同步 `python3 scripts/sync-i18n-from-excel.py`（不写 uiTextZhCN）。禁硬编码 UI；禁 `ui(member.name)`。

---

## 6. 样式与展示

### 6.1 Token

查 `scale/semantic.json`、`typography/semantic.json`、`@eds/desktop-tokens/json`。`--material-*`/`--text-*`/`--stroke-*`；hover `--motion-recipe-hover`；click/focus/selected 禁 transition。

### 6.2 千分位

展示态→`formatGroupedNumber` 或 `formatGroupedDisplay.ts`。禁 `toLocaleString`；不格式化输入态、倒计时 `MM:SS`、ID（`SIG-*`）。

### 6.3 顶底 scrim

EDS §13/`Skid.vue`；本仓 `useScrollChromeScrim.ts`。Layout→`EgLayout #skid`+`EgSkid`；Popup/Flotation→scrim composable。单滚动容器；背景 `effect-mask`+`eds-blur-bg`；Flotation 底缘→`mask-image` bottom fade。禁 fixed header/不透明 scrim/盖 EgSkid/祖先 transform 包滚动。顶未滚时顶栏实色=正确。改 mask→eds-desktop `effect-mask` token。

### 6.4 网络名

链 Tag 用链全名；币种只在金额列。禁 `BSC`、`USDT-Ethereum` 等。批处理：`Ethereum Mainnet`、`The Open Network`、`Tron`、`Bitcoin`。

### 6.5 Layout 列表↔整页详情

非 `EgDetailPopup` 的整页详情。CSS：`layoutChromePage.shared.module.css`、`layoutChromeDetailHost.shared.module.css`。

同一 `EgLayout`+`#toolbar` 切换；禁详情再套 Layout/固定顶栏/EgPopup/无动效硬切。详情 ToolBar `show-back`；`:show-paginer` 仅列表 true。`motion-page`（`LayoutChromePageStack`+`useMotionPageTransition`）。详情根 `layoutChromeScroll`；禁手写 padding-top。列表 DataList 底分页→`useLayoutChromeInset`。

整页 `EgDetail`：`show-toolbar=false`；`layoutChromeDetailHost`（藏关闭钮、防塌高、Headline 顶距）；禁 Popup 关闭 X。内嵌 DataList 少量行→按行定高（`fill=false`+`computeBatchDataListHeight`+`batchDetailDataListSized`）；禁无条件 fill blank 行；`fill=true` 仅 Popup 子页全高列表。

Popup 内 motion-page：正文+底栏同 `<Transition name=motion-page>`；`SigningBatchPopupMotionPageChrome`+`#footer`；`integrated-page-stack`+`content-fill` 禁外置 footer。

参考：`PaymentEngineDataListPage.vue`、`PaymentEngineBulkTransferDetailPage.vue`、`SigningBatchSignConfirmPopup.vue`。

### 6.6 EgDetail Apply_Item

Detail 行**必须** `createDetailApplyItemRow(variantId, overrides)`；无复制 catalog variant；禁 `showValueCopy`。仅可覆盖 key/title/value/tag/valueSymbolCrypto/valueIcon/valueSymbolAvatarName。参考 `buildApprovalDetailSections.ts`、EDS `detail-apply-item.md`。

---

## 7. 场景定稿

### 7.1 EgPopup

真源 `usePopupShellLifecycle.ts`（`watch: { immediate: true }`）。逻辑态 flow `*Open` vs 视觉态 `v-model:open`。

| 阶段 | 行为 |
|------|------|
| 入场 | open&&!suspended→mount→nextTick→rAF→popupOpen=true |
| 真关闭 | open=false→popupOpen=false→@close→onClosed |
| 内联关闭 | **仅** popupOpen=false |
| 暂挂 | open&&suspended→instant 卸 shell，不调 onClosed |

suspend：Detail 层→verifyOpen||viewMoreOpen（Signing 另加 progress/multiSign）。禁 visibility 暂挂；内联关闭同时 emit update:open；上层开时 v-if 卸下层。

尺寸禁自订：verify+trade-password 328×416；detail 880×620；custom md/lg/sm 780×560/880×620/656×516。Remark Popover 336；Popover 触发保持点按态。Detail：`toolbar-divider-pinned`；`:toolbar-page-key=detail.id`；验证翻下一条 `pendingAutoAdvanceId`→Verify 关后再 loadDetail。矿工费多页→`useMotionLayoutDeformPageSwitch`。

### 7.2 批签

`signing/batch/**`；mock 密码 `111111`。确认/进度 custom 780×560+`SigningBatchPopupSlotChrome`→`AppPopupOverlayHost`。多页→§6.5。多币种→Flotation+NetworkPicker（禁居中 Popup）；trigger 仅 icon metrics；410×440；scrim；过滤全量 sortedDataList 按 currencyKey；整行可点。

禁回退：密码取消只关 verifyOpen；批处理钮再点退出多选；进度任意 activeTask 可开；密码成功即 createBatchSigningTask；矿工费 profile 取 eligibility.signable[0]；切菜单 dismiss+exitSelectMode。

### 7.3 多签

joined success+设备；pending warning 无设备；offline 无 Tag。joinedCount=max(1,required-1)。浮标 click-toggle=false；开前 closeAllAnchoredTooltips；离页 v-if 卸。

### 7.4 付款钱包列

rowIndex=2 长名溢出；titleTextSlot+OverflowText，Tag 紧跟（禁 host-flex 推 Tag 到列右）。

### 7.5 Shell Debug（DEV）

只改 `dev/shell-debug/**`；业务禁 import dev。Inspect 5 规则（`inspectNamingRules.ts`）：图标宿主→组件；EDS/catalog 组件根→组件+props；文本→Text；CSS Module Figma 区域→区域名；其余→HTML。一层一名；禁祖先取名/继承。点不到父层→属性首行「祖先」；禁回流 primaryLabel。R2 覆盖任意 EDS 根（`__file`）；R4 区域禁复制 props/编造名；多角色→catalog `resolveDisplayName` hook。改后跑 verify-shell-debug-inspect-*.mjs。

### 7.6 DataList 批选

EDS `DataList.vue`/CSS/`useResponsiveColumns.ts`/`overflowMeasureDuringLayout.ts`；业务只控 selectMode/选中，**禁**重写动画或盖 DOM。

idle↔select（勾选 0↔40）两套稳定布局，CSS transition；禁动画中重跑选列/rAF 改 ref。列集+勾选列常驻 DOM；挤下列宽→0 仍挂载。selectMode 变一次更新宽；300ms=`--motion-duration-shift`=`SELECT_COLUMN_ANIM_MS`；settle 320ms；Batch Bar 同 `--motion-recipe-float`。退场 selectedList 立即清空；selectionExitSnapshot 维持 300ms。动画期 `data-eds-data-list-layout-animating` 冻结测量。reduced-motion 无 delay。

禁回退：v-if 挂卸列；exit await nextTick；visibleSlotIndices 随进度变；enter/exit 不对称；`:key` remount 普通退出（`dataListRemountKey` 仅提交后数据集换代）；业务 CSS 盖 col。

业务：`v-model:select-mode`+`@update:selected-list`；退出汇聚 `setSelectMode(false)`。

### 7.7 DataList min-width

min-width prop=硬下限；禁 flex 列 max-width；禁业务 CSS 盖 col。flex 列 min 上均分 extra；Σ min>预算（容器−尾列−80px reserve）→横滚。批选可见列宽=idle；挤下列 display-order 隐藏。min-width 仅 prop/helper；Inspect 适配只显 prop 非实测 col；切菜单 `syncTasksDataListColumnMinWidths`。

### 7.8 EgDataList mock

入口 `egDataListMockData.ts`。

| 列 | 规则 |
|----|------|
| 币种 0–7 | USDT/BSC、TON、ZEC、AAVE/Base、MNT/Solana、GBG、1INCH、DEEP；8+→resolveCurrencyRowPreset |
| 状态 | 前 N showcase 全集→resolveShowcaseThenCompletedStatus；其后全完成态 |
| 金额 | resolveDemoAmountRowValues；前 8 preset，9+ seeded |
| 地址/hash | 已验证池；禁随机 hex/全表同值 |
| 编号/时间 | 按 rowIndex 派生 |

---

## 8. 校验与 PR

```shell
pnpm typecheck && pnpm build
node scripts/verify-list-field-crypto.mjs
node scripts/verify-miner-fee-profiles.mjs
node scripts/verify-shell-debug-inspect-{naming,catalog,effect}.mjs
```

PR：无 website/showcase 复制；无 Eg* 根覆盖/私造 token；EgFlotation+Popup lifecycle+内联关闭仅 popupOpen=false；ui 标签非 value；千分位；scrim；§6.5 整页详情+Apply_Item；链全名；§7.6 批选常驻；§7.7 min-width；§7.8 mock；dev HMR；listFieldCrypto 扩展；EDS→远程+pin（§2.5）。

---

文档 **3.2**（2026-09-18 入口迁至 `agents.mdc`；协作/硬约束速查在入口，详案在本文件）。违反项须 PR 修正或维护者书面例外。
